[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
markers =
    external: performs a real call to an external LLM/embedding provider (network + API key required); excluded by default, run explicitly with `pytest -m external`
addopts = -m "not external"
